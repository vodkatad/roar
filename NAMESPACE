#exportPattern("^[[:alpha:]]+") # Exports all alphanumeric variables (and S4 classes from 2.13.0). Not suggested in production code.

import(GenomicRanges, Rsamtools, methods, GenomicAlignments)
importFrom(rtracklayer, import)
importFrom(GenomicRanges, SummarizedExperiment, GRanges, assays, assay, rowData, colData, qwidth)
importFrom(GenomicAlignments, readGAlignments, summarizeOverlaps)

exportClasses("RoarDataset")

export(
  RoarDataset,
  RoarDatasetFromFiles
)

exportMethods(
   countPrePost,
   computeRoars,
   computePvals,
   totalResults,
   fpkmResults,
   countResults,
   standardFilter,
   pvalueFilter
#   cores
#  "cores<-"
)

