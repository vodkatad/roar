exportPattern("^[[:alpha:]]+") # Exports all alphanumeric variables (and S4 classes from 2.13.0). Not suggested in production code.

import(GenomicRanges, rtracklayer, Rsamtools) # importFrom(foo, f, g) to be more specific.

exportClasses("RoarDataset")

export(
  RoarDataset 
)

exportMethods(
   countPrePost,
   computeRoars,
   computePvals,
   totalResults,
   fpkmResults,
   countResults,
   standardFilter,
   pvalueFilter
#   cores
#  "cores<-"
)

